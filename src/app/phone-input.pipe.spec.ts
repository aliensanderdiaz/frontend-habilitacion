import { PhoneInputPipe } from './phone-input.pipe';

describe('PhoneInputPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneInputPipe();
    expect(pipe).toBeTruthy();
  });
});
